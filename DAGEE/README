-- Add this repo as a submodule of the repo/project that needs hipDAGGER functionality
  `git submodule add <url>`
  `git submodule init`
  `git submodule update`

-- This repo depends on following other repos that should be added as
submodules to the project that needs hipDAGGER functionality. The dependent
repos are:
  -- cmakeUtils git@gitlab1.amd.com:AsyncTasking/cmakeUtils.git
  -- cppUtils git@gitlab1.amd.com:AsyncTasking/cppUtils.git 
